# 🏗️ Kafka Architecture Diagram

## 📊 Overall Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         NestJS Application                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    App Module                                │  │
│  │  ┌────────────────────┐  ┌──────────────────────────────┐   │  │
│  │  │ KafkaProducerModule│  │ KafkaConsumerIndexModule     │   │  │
│  │  │    (@Global)       │  │                              │   │  │
│  │  └────────────────────┘  └──────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                  Business Services                           │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │  │
│  │  │ MovieService │  │ UserService  │  │ OtherService │      │  │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │  │
│  │         │                 │                 │               │  │
│  │         └─────────────────┴─────────────────┘               │  │
│  │                           │                                 │  │
│  │                           ▼                                 │  │
│  │              ┌────────────────────────┐                     │  │
│  │              │ KafkaProducerService   │ ◄── Singleton       │  │
│  │              │  (Single Instance)     │                     │  │
│  │              └────────────┬───────────┘                     │  │
│  └───────────────────────────┼──────────────────────────────────┘  │
└────────────────────────────────┼─────────────────────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Kafka Cluster       │
                    │  ┌─────────────────┐  │
                    │  │ movie-events    │  │
                    │  ├─────────────────┤  │
                    │  │ movie-views     │  │
                    │  ├─────────────────┤  │
                    │  │ user-actions    │  │
                    │  ├─────────────────┤  │
                    │  │ notification-*  │  │
                    │  └─────────────────┘  │
                    └───────────┬───────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
    ┌─────────────────┐ ┌─────────────┐ ┌──────────────┐
    │ MovieConsumer   │ │ Analytics   │ │ Notification │
    │                 │ │ Consumer    │ │ Consumer     │
    │ Topics:         │ │             │ │              │
    │ - movie-events  │ │ Topics:     │ │ Topics:      │
    │ - movie-views   │ │ - movie-    │ │ - notif-*    │
    │                 │ │   views     │ │              │
    │ Group:          │ │ - user-     │ │ Group:       │
    │ movie-group     │ │   actions   │ │ notif-group  │
    │                 │ │             │ │              │
    │                 │ │ Group:      │ │              │
    │                 │ │ analytics-  │ │              │
    │                 │ │ group       │ │              │
    └─────────────────┘ └─────────────┘ └──────────────┘
```

---

## 🔄 Message Flow

### Producer Flow (Sending Events)

```
┌─────────────┐
│ Controller  │
│   or        │
│  Service    │
└──────┬──────┘
       │
       │ 1. Call method
       │    (e.g., sendMovieViewEvent)
       ▼
┌──────────────────────┐
│ KafkaProducerService │
│  (Singleton)         │
└──────┬───────────────┘
       │
       │ 2. Format message
       │    Add timestamp, key
       ▼
┌──────────────────────┐
│  Kafka Producer      │
│  (kafkajs)           │
└──────┬───────────────┘
       │
       │ 3. Send to broker
       ▼
┌──────────────────────┐
│  Kafka Broker        │
│  Topic: movie-events │
└──────────────────────┘
```

### Consumer Flow (Receiving Events)

```
┌──────────────────────┐
│  Kafka Broker        │
│  Topic: movie-events │
└──────┬───────────────┘
       │
       │ 1. Poll messages
       ▼
┌──────────────────────┐
│  Kafka Consumer      │
│  (kafkajs)           │
└──────┬───────────────┘
       │
       │ 2. Receive message
       ▼
┌──────────────────────────┐
│ BaseKafkaConsumerService │
│  - Connection mgmt       │
│  - Error handling        │
│  - Message parsing       │
└──────┬───────────────────┘
       │
       │ 3. Call handleMessage()
       ▼
┌──────────────────────────┐
│ MovieConsumerService     │
│  - Route by event type   │
│  - Business logic        │
│  - Update database       │
└──────────────────────────┘
```

---

## 🏛️ Class Hierarchy

```
┌────────────────────────────────┐
│ BaseKafkaConsumerService       │
│  (Abstract Base Class)         │
│                                │
│  + constructor(options, name)  │
│  + onModuleInit()              │
│  + onModuleDestroy()           │
│  - connect()                   │
│  - disconnect()                │
│  # handleMessage() [abstract]  │
│  # handleError()               │
│  # parseMessage<T>()           │
└────────────┬───────────────────┘
             │
             │ extends
             │
    ┌────────┴────────┬─────────────────┐
    │                 │                 │
    ▼                 ▼                 ▼
┌─────────┐   ┌──────────────┐  ┌──────────────┐
│ Movie   │   │ Analytics    │  │ Notification │
│Consumer │   │ Consumer     │  │ Consumer     │
│         │   │              │  │              │
│Service  │   │ Service      │  │ Service      │
└─────────┘   └──────────────┘  └──────────────┘
```

---

## 📦 Module Dependencies

```
┌─────────────────────────────────────────────────┐
│              AppModule                          │
└─────────────────┬───────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌───────────────┐   ┌──────────────────────┐
│ Producer      │   │ Consumer Index       │
│ Module        │   │ Module               │
│ (@Global)     │   └──────────┬───────────┘
└───────────────┘              │
                               │
                    ┌──────────┼──────────┐
                    │          │          │
                    ▼          ▼          ▼
            ┌────────┐  ┌──────────┐  ┌──────┐
            │ Movie  │  │Analytics │  │ ...  │
            │Consumer│  │Consumer  │  │      │
            │Module  │  │Module    │  │      │
            └────────┘  └──────────┘  └──────┘
```

---

## 🎯 Event Flow Example

### Example: User watches a movie

```
1. User Action
   ┌─────────────┐
   │   Browser   │
   └──────┬──────┘
          │ HTTP Request
          ▼
   ┌─────────────┐
   │ Controller  │
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │MovieService │
   └──────┬──────┘
          │
          │ trackView(userId, movieId)
          ▼

2. Producer Sends Event
   ┌──────────────────────┐
   │ KafkaProducerService │
   └──────┬───────────────┘
          │
          │ sendMovieViewEvent()
          ▼
   ┌──────────────────────┐
   │  Kafka Broker        │
   │  Topic: movie-views  │
   └──────┬───────────────┘
          │
          │ Broadcast to all consumers
          │ in different groups
          │
    ┌─────┴─────┐
    │           │
    ▼           ▼

3. Multiple Consumers Process
   ┌─────────────┐     ┌──────────────┐
   │   Movie     │     │  Analytics   │
   │  Consumer   │     │  Consumer    │
   └──────┬──────┘     └──────┬───────┘
          │                   │
          │                   │
          ▼                   ▼
   Update view count    Store analytics
   in database          data
```

---

## 🔐 Consumer Groups

```
Topic: movie-views
├── Partition 0 ─┬─► Consumer A (movie-group)
│                └─► Consumer X (analytics-group)
│
├── Partition 1 ─┬─► Consumer B (movie-group)
│                └─► Consumer Y (analytics-group)
│
└── Partition 2 ─┬─► Consumer C (movie-group)
                 └─► Consumer Z (analytics-group)

Note: 
- Same group = Load balancing (only one consumer gets message)
- Different groups = Broadcasting (all groups get message)
```

---

## 📁 File Structure

```
kafka/
│
├── 📄 README.md              ← Documentation
├── 📄 MIGRATION_GUIDE.md     ← Migration instructions
├── 📄 USAGE_EXAMPLES.ts      ← Code examples
├── 📄 ARCHITECTURE.md        ← This file
│
├── 📁 config/
│   └── kafka.config.ts       ← Centralized config
│
├── 📁 producer/
│   ├── kafka-producer.module.ts
│   └── kafka-producer.service.ts  ← Singleton producer
│
└── 📁 consumer/
    ├── kafka-consumer-index.module.ts  ← Aggregator
    │
    ├── 📁 base/
    │   ├── kafka-consumer.interface.ts
    │   └── kafka-consumer-base.service.ts  ← Base class
    │
    ├── 📁 movie/
    │   ├── kafka-consumer-movie.module.ts
    │   └── kafka-consumer-movie.service.ts
    │
    └── 📁 analytics/
        ├── kafka-consumer-analytics.module.ts
        └── kafka-consumer-analytics.service.ts
```

---

## 🎨 Design Patterns Used

### 1. **Singleton Pattern**
- `KafkaProducerService` - Only one instance

### 2. **Template Method Pattern**
- `BaseKafkaConsumerService` - Defines skeleton, subclasses implement details

### 3. **Dependency Injection**
- NestJS DI container manages all services

### 4. **Observer Pattern**
- Kafka pub/sub mechanism

### 5. **Factory Pattern**
- Dynamic consumer creation based on config

---

## 🚀 Scalability

### Horizontal Scaling

```
┌─────────────────────────────────────────┐
│          Load Balancer                  │
└─────────────┬───────────────────────────┘
              │
    ┌─────────┼─────────┬─────────┐
    │         │         │         │
    ▼         ▼         ▼         ▼
┌────────┐┌────────┐┌────────┐┌────────┐
│ App    ││ App    ││ App    ││ App    │
│Instance││Instance││Instance││Instance│
│   1    ││   2    ││   3    ││   4    │
└────┬───┘└────┬───┘└────┬───┘└────┬───┘
     │         │         │         │
     │    Same Producer (Singleton in each instance)
     │         │         │         │
     └─────────┴─────────┴─────────┘
               │
               ▼
        ┌─────────────┐
        │   Kafka     │
        └─────────────┘
               │
     ┌─────────┴─────────┬─────────┐
     │                   │         │
     ▼                   ▼         ▼
Consumer Group 1    Consumer    Consumer
(movie-group)       Group 2     Group 3
- Instance 1        (analytics) (notif)
- Instance 2        - Inst 1    - Inst 1
- Instance 3        - Inst 2    - Inst 2
- Instance 4
```

**Key Points:**
- Each app instance has its own producer
- Consumers in same group share load
- Different groups process independently

---

## 💡 Best Practices Implemented

1. ✅ **Single Responsibility** - Each consumer handles one domain
2. ✅ **Open/Closed Principle** - Easy to extend, no need to modify
3. ✅ **Dependency Inversion** - Depend on abstractions (base class)
4. ✅ **DRY** - Base class eliminates code duplication
5. ✅ **Separation of Concerns** - Producer/Consumer/Config separated
6. ✅ **Type Safety** - TypeScript interfaces everywhere
7. ✅ **Error Handling** - Graceful degradation
8. ✅ **Logging** - Comprehensive logging for debugging
9. ✅ **Scalability** - Horizontal scaling ready
10. ✅ **Maintainability** - Clear structure, easy to understand

---

**Last Updated**: 2025-11-22  
**Version**: 2.0.0
